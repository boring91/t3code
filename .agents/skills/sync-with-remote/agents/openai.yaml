interface:
  display_name: "Sync With Remote"
  short_description: "Sync upstream and build both releases"
  default_prompt: "Use $sync-with-remote to sync this fork with upstream and build the DMG and Release IPA."

policy:
  allow_implicit_invocation: false
