interface:
  display_name: "Sync With Remote"
  short_description: "Sync, release, deploy, and push the fork"
  default_prompt: "Use $sync-with-remote to sync this fork, build and deploy all release artifacts, then push the validated branch to my fork."

policy:
  allow_implicit_invocation: false
